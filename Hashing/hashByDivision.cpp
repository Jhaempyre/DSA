//there could be condition in hashing when hl edieviding we have collisons going on inorder to have this we want 


#include<iostream>
#include<map>

using namespace std;


int main(){
    map<int,int> m;
    int n ; 
    cout<<"enter number of array elemnts";
    cin>>n;
    int arr[n];
    for(int i=0;i<n;i++)
    {
        cin>>arr[i];
        int x = arr[i];
        int key = x%10;
        

    }   

}
